# Database connection string (PostgreSQL)
DB_CONNECTION_STRING=postgresql://username:password@host:5432/database?sslmode=require

# Read-only connection for hackathon participants (provided separately)
# DB_CONNECTION_STRING_READONLY=postgresql://readonly_user:password@host:5432/database?sslmode=require
